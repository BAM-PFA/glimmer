<% value = presenter.thumbnail.render(@image_options) %>

<% if value %>
  <div class="document-thumbnail">
    <%= helpers.link_to_document(presenter.document, value, counter: @counter) %>
  </div>
<% end %>
